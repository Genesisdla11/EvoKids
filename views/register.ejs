<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro - Plataforma de Terapia</title>
    <!-- Enlaza la hoja de estilos CSS -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>Registro de Usuario</h1>
        <nav>
            <ul>
                <li><a href="/">Inicio</a></li>
                <li><a href="/login">Inicio de sesion</a></li>
            </ul>
        </nav>
    </header>
    <main class="register-container">
        <!-- Sección para mostrar mensajes flash (éxito/error) -->
        <% if (messages.success && messages.success.length > 0) { %>
            <ul class="flashes">
                <li class="success"><%= messages.success[0] %></li>
            </ul>
        <% } %>
        <% if (messages.error && messages.error.length > 0) { %>
            <ul class="flashes">
                <li class="error"><%= messages.error[0] %></li>
            </ul>
        <% } %>

        <!-- Formulario de registro -->
        <!-- 'method="POST"' indica que los datos se enviarán en el cuerpo de la solicitud. -->
        <!-- 'action="/register"' indica que los datos se enviarán a la ruta '/register' del servidor. -->
        <form method="POST" action="/register">
            <div class="form-group">
                <label for="username">Nombre de Usuario:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div class="form-group">
                <label for="password">Contraseña:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <div class="form-group checkbox-group">
                <!-- 'name="is_parent"' es el nombre que el servidor usará para acceder a este valor. -->
                <input type="checkbox" id="is_parent" name="is_parent">
                <label for="is_parent">Soy un Padre/Tutor</label>
            </div>
            <button type="submit" class="button">Registrarse</button>
        </form>
    </main>
    <footer>
        <p>&copy; 2025 Mi Plataforma de Terapia. Todos los derechos reservados.</p>
    </footer>
</body>
</html>